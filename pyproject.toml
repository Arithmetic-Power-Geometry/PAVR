[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"

[project]
name="pavr"
version="0.1.0"
description="Personalized Adaptive Virtual Reality for early cybersickness prediction and minimum-disruption intervention."
requires-python=">=3.10"

[tool.pytest.ini_options]
testpaths=["tests"]
